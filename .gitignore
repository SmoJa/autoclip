# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/

# ML model files — generated locally, not committed
*.onnx
*.pth
*.tflite
panns_data/

# Logs
*.log
/tmp/autoclip.log

# User config and cache — machine-specific
~/.config/autoclip/
~/.cache/autoclip/

# Claude Code project instructions
.claude/

# Dev-only test files — never commit
dev/

# Experimental plugins — gitignored until ready for release
# To release a plugin, remove its entry here and commit the file.
autoclip/audio_triggers/poe2_loot_drops.py
autoclip/audio_triggers/poe2_loot_drops_sounds/
autoclip/games/poe2.py

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Distribution / packaging
dist/
build/
*.egg-info/
# Release artifacts (installer .exe + loose-source zips) — attached to GitHub
# releases, not committed.
release/

# AI working notes / context dumps — not part of the app
claude-context/

# Bundled OBS runtime (libobs + embeddable python) — assembled at package time
# from an OBS install + the Python embeddable zip, not committed (~80 MB of binaries).
obs-runtime/

# Rust build output
target/

