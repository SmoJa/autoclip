PyQt6>=6.4.0
sounddevice>=0.4.6
numpy>=1.24.0
onnxruntime>=1.16.0
requests>=2.28.0
send2trash>=1.8.0
# Optional: python-mpv requires libmpv.dll — install separately if you want in-app clip playback
# Optional: pynput — manual hotkey support (install if needed)
